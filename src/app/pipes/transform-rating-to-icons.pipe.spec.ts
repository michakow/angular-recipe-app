import { TransformRatingToIconsPipe } from './transform-rating-to-icons.pipe';

describe('TransformRatingToIconsPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformRatingToIconsPipe();
    expect(pipe).toBeTruthy();
  });
});
